<?php
    namespace Number;

    define("STOCK",100.10000);
    function stock($i,$q){
        return $i + $q;
    }


     # Notes = PHP can't acept the same function name
?>


<h2>Namespaces = To use the same function name in PHP</h2>
<!------------------
* Namespaces are declared at the beginning of a file using the namespace keyword
* A namespace declaration must be the first thing in the PHP file
----------------------->